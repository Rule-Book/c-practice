#include <stdio.h>
#include <limits.h>

/* print appropriate values from standard headers of 
 * 	char
 * 	short
 * 	int */
int main()
{
	printf("min to max range of \tu-char\t%u-%u\n", 0, UCHAR_MAX);
	printf("min to max range of \tchar\t%d-%d\n", SCHAR_MIN, SCHAR_MAX);

	printf("min to max range of \tu-short\t%u-%u\n", 0, USHRT_MAX);
	printf("min to max range of \tshort\t%d-%d\n", SHRT_MIN, SHRT_MAX);

	printf("min to max range of \tu-int\t%u-%u\n", 0, UINT_MAX);
	printf("min to max range of \tint\t%d-%d\n", INT_MIN, INT_MAX);
	// printf("min to max range of \tchar\t\n", );
	// printf("min to max range of \tchar\t\n", );
	// printf("min to max range of \tchar\t\n", );
	// printf("min to max range of \tchar\t\n", );
	// printf("min to max range of \tchar\t\n", );
	// printf("min to max range of \tchar\t\n", );
	// printf("min to max range of \tchar\t\n", );
	// printf("min to max range of \tchar\t\n", );
}
